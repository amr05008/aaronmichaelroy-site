---
import '../styles/global.css';
import Analytics from '@vercel/analytics/astro';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  canonicalURL?: string;
}

const { title, description, ogImage, canonicalURL } = Astro.props;
const siteTitle = 'Aaron Roy';
const siteDescription = 'Product manager, writer, and tech enthusiast';
const fullTitle = title === siteTitle ? title : `${title} | ${siteTitle}`;
const metaDescription = description || siteDescription;
const currentURL = canonicalURL || new URL(Astro.url.pathname, Astro.site).toString();
// Remove OG image reference until image is created
const defaultOGImage = ogImage || null;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/zuko_favicon.svg" />

    <!-- Primary Meta Tags -->
    <title>{fullTitle}</title>
    <meta name="title" content={fullTitle} />
    <meta name="description" content={metaDescription} />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={currentURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={currentURL} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={metaDescription} />
    {defaultOGImage && <meta property="og:image" content={defaultOGImage} />}

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={currentURL} />
    <meta property="twitter:title" content={fullTitle} />
    <meta property="twitter:description" content={metaDescription} />
    {defaultOGImage && <meta property="twitter:image" content={defaultOGImage} />}
  </head>
  <body class="min-h-screen bg-white text-gray-900 antialiased">
    <header class="border-b border-gray-200">
      <nav class="max-w-3xl mx-auto px-6 py-8 flex justify-between items-center">
        <a href="/" class="text-xl font-semibold tracking-tight hover:text-gray-600 transition-colors">{siteTitle}</a>
        <div class="flex gap-8 text-sm">
          <a href="/writing" class="hover:text-gray-600 transition-colors">Writing</a>
          <a href="/about" class="hover:text-gray-600 transition-colors">About</a>
        </div>
      </nav>
    </header>

    <main>
      <slot />
    </main>

    <footer class="border-t border-gray-200 mt-24">
      <div class="max-w-3xl mx-auto px-6 py-12 text-sm text-gray-600">
        <p>&copy; {new Date().getFullYear()} Aaron Michael Roy</p>
      </div>
    </footer>
    <Analytics />
  </body>
</html>
